# YSH
YSH (Yousuf Shell) is a Java-based CLI + GUI shell featuring custom file system operations, themed interface, executable packaging, and a built-in network chat system for multi-user communication.
